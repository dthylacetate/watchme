# 分支策略

当前阶段建议只在 `main` 一个分支里工作。

原因：

- 现在的重点是整理上游素材、明确架构、搭建新项目骨架，单分支推进更省心。
- 上游的 `main`、`windows-source`、`macos-source`、`redesign/*` 不适合直接 Git merge 到一起；它们有不同用途、不同目录边界，redesign 分支还包含构建产物和删改过的功能。
- 当前仓库已经把有用分支作为源码快照放在 `references/upstream-branches/`，后续可以按需拷贝、重写或对照，而不是背着复杂历史一起走。

工作方式：

1. `main` 保存所有分析、参考快照和重构代码。
2. 新项目代码放在 `refactor/`。
3. 上游内容只作为参考，不直接在原快照目录里开发。
4. 如果之后出现较大的实验，例如新 UI 或 Agent 重写，再临时创建短生命周期分支，验证后合回 `main`。

一句话：现在先保持一个干净主线，等新项目形状稳定后再按功能拆分支。

